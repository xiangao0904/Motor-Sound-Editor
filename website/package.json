{
  "name": "motor-sound-editor-website",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev . --host 127.0.0.1 --port 4173",
    "build": "vitepress build .",
    "preview": "vitepress preview . --host 127.0.0.1 --port 4174",
    "typecheck": "pnpm --dir .. exec vue-tsc --noEmit -p website/tsconfig.json"
  },
  "devDependencies": {
    "vitepress": "^1.0.0"
  }
}
